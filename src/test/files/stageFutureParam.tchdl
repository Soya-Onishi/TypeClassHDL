package test

module Top
impl Top {
  input def function(future: Future[Bit[2]]) -> Future[Bit[2]] {
    generate st(future)
  }

  stage st(in: Future[Bit[2]]) -> Future[Bit[2]] {
    return in
  }
}